# Optional Docker Compose overrides for local development only.
# Generate fresh values before using a shared development environment.
AUTH_BEARER_HMAC_SECRET=
AUTH_MFA_ENCRYPTION_KEY=
AUTH_RECOVERY_CODE_PEPPER=
